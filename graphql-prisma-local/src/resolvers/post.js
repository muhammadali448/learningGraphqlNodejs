const post = {
  // author: (parent, arg, { db: { users } }, info) =>
  //   users.find((f) => f.id === parent.author),
  // comments: (parent, args, { db: { comments } }, info) =>
  //   comments.filter((comment) => comment.post === parent.id),
};

export default post;
